# Copy to .env for local dev. Vite only exposes vars prefixed with VITE_ to the
# client, and inlines them at build time (mirrors the Flutter --dart-define flow).
# Use the same Supabase project values the Flutter app uses (see root .env).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
