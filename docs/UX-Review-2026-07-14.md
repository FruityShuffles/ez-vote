# UX Review — 2026-07-14

Synthetic M18 review of the deployed React staging app (`ez-vote-react.pages.dev`)
against the frozen Flutter reference. Reviewed as a signed-out visitor and with a
throwaway test account at desktop and 390×844 mobile widths.

## Scope and outcome

The reviewed public landing, election-creation, and STAR + FPTP ballot surfaces
had no actionable usability, responsiveness, or accessibility findings. The
React landing now uses the Flutter logo artwork; the STAR ballot keeps score
controls, auto-selected FPTP first choice, and submit action visible without
horizontal overflow at both reviewed widths.

| Screen | Viewport | Finding | Heuristic | Severity | Suggested fix |
|---|---|---|---|---|---|
| Landing | Desktop + mobile | No actionable finding. Semantic heading, CTA links, and footer links match the visible Flutter flow. | Visibility, consistency, accessibility | — | — |
| Create election | Mobile | No actionable finding. Labels, candidate controls, algorithm choices, and settings remain readable and operable within the viewport. | Responsiveness, error prevention | — | — |
| STAR + FPTP results | Desktop + mobile | No actionable finding. The two method cards are visually distinct side-by-side at desktop, then stack without clipping or lost content at 390×844. | Hierarchy, responsiveness | — | — |
| STAR + FPTP ballot | Desktop + mobile | No actionable finding. Each score group has an accessible label; the sole top-scored candidate is visibly auto-selected as FPTP first choice; back and submit controls remain clear. | Match to task, accessibility, user control | — | — |
| STAR + IRV reranking | Desktop + mobile | No actionable finding. Raising Bob from 0→5 and Cara from 0→4 visibly slid each row into its derived score position rather than snapping; controls and row order remained legible at 390px. | Visibility of system status, responsiveness | — | — |

This is a bounded visual review. The detailed M18 verification record remains
in [[Migration/Parity Checklist]].
